{
    plugins.neo-tree = {
      enable = true;
      closeIfLastWindow = false;
      hideRootNode = true;

      filesystem = {
        followCurrentFile.enabled = true;
      };

      window = {
        position = "right";
      };
    };
}
